{
  "name": "segmentio-snippet-lite",
  "version": "3.1.1",
  "repository": "git://github.com/jordanh/segmentio-snippet-lite",
  "description": "Simple ES6 templating method for rendering the analytics.js snippet.",
  "scripts": {
    "clean": "rimraf dist",
    "build": "babel --ignore lib/snippet.js -d dist/ lib/ && cp lib/snippet.js dist/",
    "prepublish": "npm run clean && npm run build"
  },
  "devDependencies": {
    "babel-cli": "6.9.0",
    "babel-plugin-transform-runtime": "^6.9.0",
    "babel-preset-es2015": "6.9.0",
    "babel-preset-stage-0": "6.5.0",
    "babel-register": "^6.9.0",
    "babel-runtime": "^6.9.2",
    "rimraf": "2.5.2"
  },
  "main": "dist/index.js",
  "dependencies": {
  }
}
